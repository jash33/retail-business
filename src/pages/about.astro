---
/**
 * About Page - Main Street Boutique
 * A comprehensive page establishing credibility and local Houston connection.
 *
 * Features:
 * - Professional introduction with photo placeholder
 * - Retail expertise and sourcing philosophy showcase
 * - Personal touch and community involvement
 * - Houston local connection emphasis
 * - Full SEO optimization with structured data
 * - WCAG 2.1 AA accessible
 */

import Layout from '../layouts/Layout.astro';
import Button from '../components/Button.astro';

const base = import.meta.env.BASE_URL;

/**
 * Expertise organized by category for the about section
 */
const skillCategories = [
  {
    title: 'Curated Selection',
    skills: [
      { name: 'Artisan Ceramics & Pottery', years: 10 },
      { name: 'Handcrafted Textiles & Linens', years: 10 },
      { name: 'Unique Home Decor Pieces', years: 8 },
      { name: 'Thoughtful Gift Collections', years: 7 },
    ],
  },
  {
    title: 'Sourcing & Quality',
    skills: [
      { name: 'Local Artisan Partnerships', years: 10 },
      { name: 'Sustainable & Ethical Sourcing', years: 8 },
      { name: 'Quality Materials & Craftsmanship', years: 10 },
      { name: 'Small-Batch & Limited Editions', years: 6 },
    ],
  },
  {
    title: 'Customer Experience',
    skills: [
      { name: 'Personal Styling & Recommendations', years: 9 },
      { name: 'Custom Gift Wrapping', years: 8 },
      { name: 'Interior Styling Consultations', years: 5 },
      { name: 'Loyalty & Rewards Program', years: 4 },
    ],
  },
];

/**
 * Key achievements and stats to display
 */
const achievements = [
  { number: '10+', label: 'Years in Business' },
  { number: '200+', label: 'Local Artisans Supported' },
  { number: '5,000+', label: 'Happy Customers' },
  { number: '1', label: 'Passionate Community' },
];

/**
 * Community involvement and personal interests
 */
const communityHighlights = [
  {
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>`,
    title: 'Houston Born & Raised',
    description: "This city is home. I know our neighborhoods, our tastes, and the talented makers who call Houston home — and I bring them all together under one roof.",
  },
  {
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>`,
    title: 'Community Builder',
    description: 'From hosting artisan pop-up markets to sponsoring local school art programs, we believe in investing in the creative spirit of our community.',
  },
  {
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>`,
    title: 'Shop Local Champion',
    description: "Every purchase at Main Street Boutique supports independent artisans and makers. We're proud to keep dollars in our local economy and craftsmanship alive.",
  },
];

/**
 * Fun facts to add personality
 */
const funFacts = [
  "I can spot a beautifully glazed ceramic from across a crowded craft fair",
  "Weekend mornings start at the Heights farmers market — always scouting for new makers",
  "Die-hard Texans fan — yes, even through the tough seasons",
  "My house is basically a showroom for items I couldn't resist bringing home from the shop",
];
---

<Layout
  title="About"
  description="Meet the owner behind Main Street Boutique. Based in Houston, we've spent over 10 years curating artisan goods and supporting local makers."
  openGraph={{
    type: 'profile',
  }}
  jsonLd={{
    type: 'AboutPage',
    name: 'About Main Street Boutique',
    description: 'Learn about the passion, expertise, and local commitment behind Main Street Boutique — your Houston destination for curated artisan goods.',
  }}
>
  <main>
    <!-- Hero Section -->
    <section class="about-hero" aria-labelledby="about-hero-heading">
      <div class="about-hero__container">
        <span class="about-hero__eyebrow reveal reveal--up">Our Story</span>
        <h1 id="about-hero-heading" class="about-hero__title reveal reveal--up reveal--delay-1">
          Your Houston<br />Artisan Boutique
        </h1>
        <p class="about-hero__subtitle reveal reveal--up reveal--delay-2">
          Curating beautiful, handcrafted goods from local artisans since 2014.
          We're not just a shop — we're your neighbors.
        </p>
      </div>
    </section>

    <!-- Personal Introduction Section -->
    <section class="about-intro" aria-labelledby="about-intro-heading">
      <div class="about-intro__container">
        <div class="about-intro__content">
          <div class="about-intro__photo-wrapper reveal reveal--left">
            <!-- Professional Photo Placeholder -->
            <div class="about-intro__photo" role="img" aria-label="Professional headshot of the Main Street Boutique owner">
              <svg class="about-intro__photo-placeholder" viewBox="0 0 200 200" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                <rect width="200" height="200" fill="currentColor" opacity="0.1"/>
                <circle cx="100" cy="70" r="35" fill="currentColor" opacity="0.2"/>
                <ellipse cx="100" cy="160" rx="55" ry="40" fill="currentColor" opacity="0.2"/>
              </svg>
              <span class="about-intro__photo-label">Photo Coming Soon</span>
            </div>
            <div class="about-intro__badge">
              <span class="about-intro__badge-text">Est. 2014</span>
            </div>
          </div>

          <div class="about-intro__text reveal reveal--up reveal--delay-1">
            <h2 id="about-intro-heading" class="about-intro__heading">
              Hi, I'm James — and I'm Passionate About Bringing Beautiful, Handcrafted Goods to Houston
            </h2>

            <div class="about-intro__bio">
              <p>
                When I opened Main Street Boutique back in 2014, I had one simple dream: to create a
                space where talented local artisans could showcase their work and where customers could find
                truly unique, handcrafted pieces. As a lifelong Houston resident, I've always believed our
                city's creative community deserves a storefront that celebrates their craft.
              </p>

              <p>
                My love for artisan goods started long before it became my livelihood. Growing up, I was
                drawn to the stories behind handmade objects — the potter's hands shaping clay, the weaver's
                patience at the loom. That appreciation led me to travel across Texas and beyond, building
                relationships with makers whose work speaks for itself. What I discovered is that my real
                passion isn't just selling beautiful things — it's connecting people with the stories and
                the artisans behind every piece.
              </p>

              <p>
                Today, Main Street Boutique partners with over 200 independent artisans and has served
                thousands of customers right here in the greater Houston area. From the couple furnishing
                their first home with one-of-a-kind ceramics to the gift-giver searching for something
                truly meaningful, each interaction reminds me why I do this. When you shop with us,
                you're not just buying a product — you're supporting a maker, investing in quality
                craftsmanship, and taking home something with a story.
              </p>
            </div>

            <div class="about-intro__cta">
              <Button
                text="Visit Our Shop"
                href={`${base}products`}
                variant="primary"
                size="large"
              />
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Achievements Stats Section -->
    <section class="about-stats" aria-labelledby="about-stats-heading">
      <div class="about-stats__container">
        <h2 id="about-stats-heading" class="sr-only">Main Street Boutique by the Numbers</h2>
        <div class="about-stats__grid">
          {achievements.map((stat, index) => (
            <div class={`about-stats__item reveal reveal--scale-up reveal--delay-${index + 1}`}>
              <span class="about-stats__number">{stat.number}</span>
              <span class="about-stats__label">{stat.label}</span>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Skills & Expertise Section -->
    <section class="about-skills" aria-labelledby="about-skills-heading">
      <div class="about-skills__container">
        <div class="about-skills__header section-heading--decorated reveal reveal--up">
          <h2 id="about-skills-heading" class="about-skills__title">
            What We Bring to You
          </h2>
          <p class="about-skills__subtitle">
            Over the years, we've honed our eye for exceptional craftsmanship and built
            an experience that makes finding the perfect piece a joy.
          </p>
        </div>

        <div class="about-skills__grid">
          {skillCategories.map((category, index) => (
            <div class={`about-skills__category reveal reveal--up reveal--delay-${index + 1}`}>
              <h3 class="about-skills__category-title">{category.title}</h3>
              <ul class="about-skills__list" role="list">
                {category.skills.map((skill) => (
                  <li class="about-skills__item">
                    <span class="about-skills__item-name">{skill.name}</span>
                    <span class="about-skills__item-years">{skill.years}+ years</span>
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Community & Personal Touch Section -->
    <section class="about-community" aria-labelledby="about-community-heading">
      <div class="about-community__container">
        <div class="about-community__header section-heading--decorated reveal reveal--up">
          <h2 id="about-community-heading" class="about-community__title">
            Rooted in Houston
          </h2>
          <p class="about-community__subtitle">
            We're more than just a shop — we're a proud part of the Houston community.
          </p>
        </div>

        <div class="about-community__grid">
          {communityHighlights.map((item, index) => (
            <article class={`about-community__card reveal reveal--up reveal--delay-${index + 1}`}>
              <div class="about-community__icon" aria-hidden="true" set:html={item.icon} />
              <h3 class="about-community__card-title">{item.title}</h3>
              <p class="about-community__card-text">{item.description}</p>
            </article>
          ))}
        </div>

        <!-- Fun Facts -->
        <div class="about-funfacts reveal reveal--up">
          <h3 class="about-funfacts__title">A Few Fun Facts</h3>
          <ul class="about-funfacts__list" role="list">
            {funFacts.map((fact, index) => (
              <li class={`about-funfacts__item reveal reveal--left reveal--delay-${index + 1}`}>
                <span class="about-funfacts__bullet" aria-hidden="true">☕</span>
                {fact}
              </li>
            ))}
          </ul>
        </div>
      </div>
    </section>

    <!-- Call to Action Section -->
    <section class="about-cta" aria-labelledby="about-cta-heading">
      <div class="about-cta__container">
        <h2 id="about-cta-heading" class="about-cta__title reveal reveal--up">
          Ready to Discover Something Special?
        </h2>
        <p class="about-cta__description reveal reveal--up reveal--delay-1">
          Stop by the shop, browse our collection online, or reach out to learn about
          our latest arrivals and upcoming artisan events. We'd love to help you find
          the perfect piece.
        </p>
        <div class="about-cta__buttons reveal reveal--up reveal--delay-2">
          <Button
            text="Shop Our Collection"
            href={`${base}products`}
            variant="primary"
            size="large"
          />
          <Button
            text="Get in Touch"
            href={`${base}contact`}
            variant="secondary"
            size="large"
          />
        </div>
      </div>
    </section>
  </main>
</Layout>

<style>
  /* =================================================================
   * HERO SECTION
   * ================================================================= */
  .about-hero {
    padding: var(--spacing-20) var(--spacing-4) var(--spacing-16);
    background: linear-gradient(
      135deg,
      var(--color-primary-50) 0%,
      var(--color-background) 100%
    );
    text-align: center;
  }

  .about-hero__container {
    max-width: var(--container-4xl);
    margin: 0 auto;
  }

  .about-hero__eyebrow {
    display: inline-block;
    font-family: var(--font-body);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-semibold);
    color: var(--color-primary);
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing-wider);
    margin-bottom: var(--spacing-4);
  }

  .about-hero__title {
    font-family: var(--font-heading);
    font-size: var(--font-size-4xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin: 0 0 var(--spacing-4);
    line-height: var(--line-height-tight);
    letter-spacing: var(--letter-spacing-tight);
  }

  .about-hero__subtitle {
    font-family: var(--font-body);
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    max-width: 36rem;
    margin: 0 auto;
    line-height: var(--line-height-relaxed);
  }

  /* =================================================================
   * PERSONAL INTRODUCTION SECTION
   * ================================================================= */
  .about-intro {
    padding: var(--spacing-20) var(--spacing-4);
    background-color: var(--color-background);
  }

  .about-intro__container {
    max-width: var(--container-6xl);
    margin: 0 auto;
  }

  .about-intro__content {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--spacing-12);
    align-items: start;
  }

  @media (min-width: 1024px) {
    .about-intro__content {
      grid-template-columns: 300px 1fr;
      gap: var(--spacing-16);
    }
  }

  .about-intro__photo-wrapper {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .about-intro__photo {
    position: relative;
    width: 100%;
    max-width: 280px;
    aspect-ratio: 1;
    background-color: var(--color-background-subtle);
    border-radius: var(--radius-2xl);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-lg);
  }

  .about-intro__photo-placeholder {
    width: 100%;
    height: 100%;
    color: var(--color-primary);
  }

  .about-intro__photo-label {
    position: absolute;
    bottom: var(--spacing-4);
    font-family: var(--font-body);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    color: var(--color-text-tertiary);
  }

  .about-intro__badge {
    position: absolute;
    bottom: -0.75rem;
    right: 10%;
    background-color: var(--color-primary);
    color: var(--color-text-inverse);
    padding: var(--spacing-2) var(--spacing-4);
    border-radius: var(--radius-full);
    box-shadow: var(--shadow-md);
  }

  .about-intro__badge-text {
    font-family: var(--font-body);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-semibold);
    white-space: nowrap;
  }

  .about-intro__text {
    padding-top: var(--spacing-4);
  }

  .about-intro__heading {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin: 0 0 var(--spacing-6);
    line-height: var(--line-height-snug);
  }

  .about-intro__bio {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-4);
  }

  .about-intro__bio p {
    font-family: var(--font-body);
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
    margin: 0;
  }

  .about-intro__cta {
    margin-top: var(--spacing-8);
  }

  /* =================================================================
   * ACHIEVEMENTS STATS SECTION
   * ================================================================= */
  .about-stats {
    padding: var(--spacing-12) var(--spacing-4);
    background-color: var(--color-primary);
  }

  .about-stats__container {
    max-width: var(--container-5xl);
    margin: 0 auto;
  }

  .about-stats__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--spacing-8);
    text-align: center;
  }

  @media (min-width: 768px) {
    .about-stats__grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .about-stats__item {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-1);
  }

  .about-stats__number {
    font-family: var(--font-heading);
    font-size: var(--font-size-4xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-inverse);
    line-height: var(--line-height-none);
  }

  .about-stats__label {
    font-family: var(--font-body);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    color: var(--color-primary-100);
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing-wide);
  }

  /* =================================================================
   * SKILLS & EXPERTISE SECTION
   * ================================================================= */
  .about-skills {
    padding: var(--spacing-20) var(--spacing-4);
    background-color: var(--color-background-subtle);
  }

  .about-skills__container {
    max-width: var(--container-6xl);
    margin: 0 auto;
  }

  .about-skills__header {
    text-align: center;
    margin-bottom: var(--spacing-12);
  }

  .about-skills__title {
    font-family: var(--font-heading);
    font-size: var(--font-size-3xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin: 0 0 var(--spacing-4);
    line-height: var(--line-height-tight);
  }

  .about-skills__subtitle {
    font-family: var(--font-body);
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    max-width: 36rem;
    margin: 0 auto;
    line-height: var(--line-height-relaxed);
  }

  .about-skills__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--spacing-8);
  }

  @media (min-width: 768px) {
    .about-skills__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .about-skills__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .about-skills__category {
    background-color: var(--color-background);
    border-radius: var(--radius-xl);
    padding: var(--spacing-6);
    box-shadow: var(--shadow-sm);
    transition: box-shadow var(--duration-200) var(--ease-in-out);
  }

  .about-skills__category:hover {
    box-shadow: var(--shadow-md);
  }

  .about-skills__category-title {
    font-family: var(--font-heading);
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-semibold);
    color: var(--color-primary);
    margin: 0 0 var(--spacing-4);
    padding-bottom: var(--spacing-3);
    border-bottom: 2px solid var(--color-primary-100);
  }

  .about-skills__list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: var(--spacing-3);
  }

  .about-skills__item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--spacing-2) 0;
    border-bottom: 1px solid var(--color-border-light);
  }

  .about-skills__item:last-child {
    border-bottom: none;
  }

  .about-skills__item-name {
    font-family: var(--font-body);
    font-size: var(--font-size-base);
    color: var(--color-text-primary);
    font-weight: var(--font-weight-medium);
  }

  .about-skills__item-years {
    font-family: var(--font-body);
    font-size: var(--font-size-sm);
    color: var(--color-text-tertiary);
    font-weight: var(--font-weight-normal);
    white-space: nowrap;
  }

  /* =================================================================
   * COMMUNITY SECTION
   * ================================================================= */
  .about-community {
    padding: var(--spacing-20) var(--spacing-4);
    background-color: var(--color-background);
  }

  .about-community__container {
    max-width: var(--container-6xl);
    margin: 0 auto;
  }

  .about-community__header {
    text-align: center;
    margin-bottom: var(--spacing-12);
  }

  .about-community__title {
    font-family: var(--font-heading);
    font-size: var(--font-size-3xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin: 0 0 var(--spacing-4);
    line-height: var(--line-height-tight);
  }

  .about-community__subtitle {
    font-family: var(--font-body);
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    max-width: 32rem;
    margin: 0 auto;
    line-height: var(--line-height-relaxed);
  }

  .about-community__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--spacing-6);
    margin-bottom: var(--spacing-12);
  }

  @media (min-width: 768px) {
    .about-community__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .about-community__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .about-community__card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: var(--spacing-8);
    background-color: var(--color-background-subtle);
    border-radius: var(--radius-xl);
    transition: box-shadow var(--duration-200) var(--ease-in-out);
  }

  .about-community__card:hover {
    box-shadow: var(--shadow-md);
  }

  .about-community__icon {
    width: 3rem;
    height: 3rem;
    color: var(--color-primary);
    margin-bottom: var(--spacing-4);
  }

  .about-community__icon :global(svg) {
    width: 100%;
    height: 100%;
  }

  .about-community__card-title {
    font-family: var(--font-heading);
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-primary);
    margin: 0 0 var(--spacing-2);
  }

  .about-community__card-text {
    font-family: var(--font-body);
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
    margin: 0;
  }

  /* =================================================================
   * FUN FACTS SECTION
   * ================================================================= */
  .about-funfacts {
    background-color: var(--color-primary-50);
    border-radius: var(--radius-2xl);
    padding: var(--spacing-8);
  }

  .about-funfacts__title {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-primary);
    margin: 0 0 var(--spacing-6);
    text-align: center;
  }

  .about-funfacts__list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--spacing-4);
  }

  @media (min-width: 768px) {
    .about-funfacts__list {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .about-funfacts__item {
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-3);
    font-family: var(--font-body);
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
  }

  .about-funfacts__bullet {
    flex-shrink: 0;
    font-size: var(--font-size-lg);
  }

  /* =================================================================
   * CALL TO ACTION SECTION
   * ================================================================= */
  .about-cta {
    padding: var(--spacing-20) var(--spacing-4);
    background-color: var(--color-primary);
    text-align: center;
  }

  .about-cta__container {
    max-width: var(--container-3xl);
    margin: 0 auto;
  }

  .about-cta__title {
    font-family: var(--font-heading);
    font-size: var(--font-size-3xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-inverse);
    margin: 0 0 var(--spacing-4);
    line-height: var(--line-height-tight);
  }

  .about-cta__description {
    font-family: var(--font-body);
    font-size: var(--font-size-lg);
    color: var(--color-primary-100);
    line-height: var(--line-height-relaxed);
    margin: 0 0 var(--spacing-8);
    max-width: 32rem;
    margin-left: auto;
    margin-right: auto;
  }

  .about-cta__buttons {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-4);
    justify-content: center;
    align-items: center;
  }

  @media (min-width: 640px) {
    .about-cta__buttons {
      flex-direction: row;
    }
  }

  /* Override secondary button colors for dark background */
  .about-cta :global(.btn--secondary) {
    color: var(--color-text-inverse);
    border-color: var(--color-text-inverse);
  }

  .about-cta :global(.btn--secondary:hover:not(.btn--disabled)) {
    background-color: rgba(255, 255, 255, 0.1);
    border-color: var(--color-text-inverse);
    color: var(--color-text-inverse);
  }

  /* =================================================================
   * SCREEN READER ONLY
   * ================================================================= */
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  /* =================================================================
   * RESPONSIVE ADJUSTMENTS
   * ================================================================= */
  @media (max-width: 767px) {
    .about-hero {
      padding: var(--spacing-12) var(--spacing-4) var(--spacing-10);
    }

    .about-hero__title {
      font-size: var(--font-size-3xl);
    }

    .about-hero__subtitle {
      font-size: var(--font-size-base);
    }

    .about-intro {
      padding: var(--spacing-12) var(--spacing-4);
    }

    .about-intro__heading {
      font-size: var(--font-size-xl);
    }

    .about-stats {
      padding: var(--spacing-10) var(--spacing-4);
    }

    .about-stats__number {
      font-size: var(--font-size-3xl);
    }

    .about-skills {
      padding: var(--spacing-12) var(--spacing-4);
    }

    .about-skills__title {
      font-size: var(--font-size-2xl);
    }

    .about-community {
      padding: var(--spacing-12) var(--spacing-4);
    }

    .about-community__title {
      font-size: var(--font-size-2xl);
    }

    .about-funfacts {
      padding: var(--spacing-6);
    }

    .about-cta {
      padding: var(--spacing-12) var(--spacing-4);
    }

    .about-cta__title {
      font-size: var(--font-size-2xl);
    }
  }

  /* =================================================================
   * SCROLL-REVEAL ANIMATION ENHANCEMENTS
   * =================================================================
   * Additional styles to enhance reveal animations on About page
   * ================================================================= */

  /* Override decoration positioning for About page headers that use section-heading--decorated */
  .about-skills__header.section-heading--decorated,
  .about-community__header.section-heading--decorated {
    padding-top: var(--spacing-4);
  }

  /* Ensure the title and subtitle within decorated headers display properly */
  .about-skills__header.section-heading--decorated .about-skills__title,
  .about-skills__header.section-heading--decorated .about-skills__subtitle,
  .about-community__header.section-heading--decorated .about-community__title,
  .about-community__header.section-heading--decorated .about-community__subtitle {
    position: relative;
    z-index: 1;
  }

  /* Add subtle hover lift to skill categories and community cards */
  .about-skills__category,
  .about-community__card {
    transition:
      box-shadow var(--duration-200) var(--ease-in-out),
      transform var(--duration-300) var(--ease-out);
  }

  .about-skills__category:hover,
  .about-community__card:hover {
    transform: translateY(-4px);
  }

  /* Stats counter animation effect */
  .about-stats__number {
    display: inline-block;
  }

  /* Fun facts list stagger container */
  .about-funfacts__list {
    position: relative;
  }

  /* =================================================================
   * REDUCED MOTION
   * ================================================================= */
  @media (prefers-reduced-motion: reduce) {
    .about-skills__category,
    .about-community__card {
      transition: none;
    }

    .about-skills__category:hover,
    .about-community__card:hover {
      transform: none;
    }

    /* Ensure reveal elements are visible without animation */
    .reveal {
      opacity: 1;
      transform: none;
    }
  }

  /* =================================================================
   * PRINT STYLES
   * ================================================================= */
  @media print {
    .about-hero {
      background: none;
      padding: var(--spacing-8) 0;
    }

    .about-stats {
      background: none;
      border: 1px solid var(--color-border);
    }

    .about-stats__number,
    .about-stats__label {
      color: var(--color-text-primary);
    }

    .about-cta {
      display: none;
    }

    .about-intro__photo {
      box-shadow: none;
      border: 1px solid var(--color-border);
    }
  }
</style>
